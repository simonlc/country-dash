OPENSKY_CLIENT_ID=your_opensky_client_id
OPENSKY_CLIENT_SECRET=your_opensky_client_secret

# Optional production/browser setting:
# point this at your own deployed proxy that returns the same JSON shape
# as the local Vite dev relay at /api/opensky/states
VITE_OPENSKY_PROXY_URL=
